#include <stdio.h>
#include <math.h>

int main (void)
{
	float N1, N2, SOMA;
	printf ("vamos fazer uma soma, digite o primeiro numero:");
	scanf ("%f", &N1);
	printf ("por favor, digite o segundo numero:");
	scanf ("%f", &N2);
	
		SOMA = (N1 + N2);
		
	printf ("\n resultado %f", SOMA);
	
return 0;
}