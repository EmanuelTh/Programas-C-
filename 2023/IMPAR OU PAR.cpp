#include <stdio.h>

int main (void)
{
	int N, aux;
		printf ("entre com um numero");
		scanf("%i", &N);
		
		aux = N / 2;
	if (aux *2 == N)
		{
			printf ("par");	
	}
			else
		{ 
		printf ("impar");
	}
	return 0;

}