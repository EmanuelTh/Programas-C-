#include <stdio.h>
#include <math.h>

int main (void)
{
	float metro, cm;
	printf("indique a metragem que deseja converter:");
	scanf ( "%f", &metro);
	
		cm=(100*metro);
			printf("\n o total em centimetros: %f", cm);
			
	
	return 0;
}